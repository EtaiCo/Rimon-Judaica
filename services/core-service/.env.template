# Copy to services/core-service/.env and save the file (empty .env on disk will not load).
PORT=4000
SUPABASE_URL=https://gxgluavxmewuffnckrxp.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste-your-service-role-secret-here
STOREFRONT_URL=http://localhost:5173

# Required for auth tokens (use a long random string in production).
JWT_SECRET=change-me-to-a-long-random-secret
# Optional, e.g. 7d, 24h
# JWT_EXPIRES_IN=7d
