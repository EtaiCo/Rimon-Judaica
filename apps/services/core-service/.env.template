# Copy to apps/services/core-service/.env and save the file (empty .env on disk will not load).
PORT=4000
NODE_ENV=development
SUPABASE_URL=https://gxgluavxmewuffnckrxp.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste-your-service-role-secret-here
STOREFRONT_URL=http://localhost:5173

# Required for auth tokens (use a long random string in production).
JWT_SECRET=change-me-to-a-long-random-secret
# Optional: previous secret accepted only during a rotation window.
# JWT_SECRET_PREVIOUS=
# Optional override (default 7d for customers, 2h for admins).
# JWT_EXPIRES_IN=7d

# Comma-separated hostnames allowed in product image URLs (Supabase Storage / CDN).
# The Supabase host is added automatically.
# ADMIN_ALLOWED_IMAGE_HOSTS=cdn.example.com,images.example.com
